---
name: token-efficient-codex-run
description: Keep repository work scoped, evidence-based, and token-efficient.
---

# Token-efficient run

- Start with `AGENTS.md`, the project state docs, the manifest, and the smallest affected files.
- Search exact symbols and follow imports one hop at a time.
- Reuse existing patterns and stop scanning when the implementation is grounded.
- Run targeted validation first, then broader checks only when shared infrastructure changed.
- Report changed files, validation, risks, and intentionally uninspected areas.
